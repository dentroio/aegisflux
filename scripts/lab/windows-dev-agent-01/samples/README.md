# Sample lab scripts (WO-VIS-001 / WO-VIS-008)

Copy these onto `windows-dev-agent-01` under `C:\AegisLab\scripts` for controlled process and network drills. They perform minimal HTTP client calls; point `TARGET_URL` at a lab mock or safe endpoint.

- `sample_agent_runner.py` — Python “agent-like” command line for detection pack drills.
- `sample_node_agent.mjs` — Node equivalent.

Do not embed secrets. Use lab-only URLs.
